# Copy to .env and fill in. If DATABASE_URL is unset, the API uses an in-memory
# repository seeded with the same demo account as db/seed.sql — fully playable, no DB needed.
PORT=8787
# DATABASE_URL=postgresql://postgres:<YOURPASS>@localhost:5432/runblade
CORS_ORIGIN=http://localhost:5173
