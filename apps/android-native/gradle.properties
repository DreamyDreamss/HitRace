org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
org.gradle.caching=true

# Supabase config lives OUTSIDE the repo (see docs/SUPABASE_SETUP.md):
#   %USERPROFILE%\.gradle\gradle.properties  ->  SUPABASE_URL / SUPABASE_ANON_KEY
# Without them the app falls back to the local dev server at 10.0.2.2:8787.
